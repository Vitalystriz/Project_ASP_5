import { StyleSheet } from 'react-native';

export const BackButtonStyles = StyleSheet.create({
  buttonContainer: {
    position: 'absolute',
    top: 50,
    left: 20,
    zIndex: 10,
    padding: 8,
  },
});